# Drop this into your nginx server block (or include it via an include directive).
# Adjust the path if your vhost root differs from /var/www/dom-orm.erikpoehler.com.
#
# location /virtual-filesystem {
#     root /var/www/dom-orm.erikpoehler.com;
#     index index.php;
#     try_files $uri $uri/ /virtual-filesystem/index.php?$query_string;
#
#     location ~ \.php$ {
#         include fastcgi_params;
#         fastcgi_pass unix:/run/php/php8.4-fpm.sock;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#     }
# }
