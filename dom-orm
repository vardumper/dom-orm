#!/usr/bin/env php
<?php

/*
 * This file is part of DOM-ORM.
 *
 * (c) Erik Pöhler <info@erikpoehler.com>
 *
 * This source file is subject to the MIT license that is bundled
 * with this source code in the file LICENSE.
 */

error_reporting(E_ALL & ~E_DEPRECATED & ~E_USER_DEPRECATED);

set_error_handler(static function ($severity, $message, $file, $line) {
    if ($severity & error_reporting()) {
        throw new ErrorException($message, 0, $severity, $file, $line);
    }
});

$possibleFiles = [__DIR__.'/../../autoload.php', __DIR__.'/../autoload.php', __DIR__.'/vendor/autoload.php'];
$file = null;
foreach ($possibleFiles as $possibleFile) {
    if (file_exists($possibleFile)) {
        $file = $possibleFile;

        break;
    }
}

if (null === $file) {
    throw new RuntimeException('Unable to locate autoload.php file.');
}

require_once $file;

use DOM\ORM\Command\Init;
use Silly\Application;
use Symfony\Component\Console\Output\ConsoleOutputInterface;

$app = new Application('DOM-ORM CLI Tool', '0.1.0');

$app->command('init [-p|--path]', function (ConsoleOutputInterface $output, $path) {
    $output->writeln('Initializing database...');

    Init::run($path);

    $output->writeln('Diagnostics output has been copied to your clipboard.');
});

$app->setDefaultCommand('init');
$app->run();

__HALT_COMPILER();