<?php
declare(strict_types=1);

uses()
    ->in('Unit');

uses()
    ->group('benchmark')
    ->in('Benchmark');

uses()
    ->group('integration')
    ->in('Integration');

uses()
    ->group('external-bin')
    ->in('Unit/Vcs');
